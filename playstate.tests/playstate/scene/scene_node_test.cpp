#include <playstate/scene/scene_node.h>
#include "../test/test.h"
using namespace playstate;

TEST_SUITE(SceneNode)
{
	UNIT_TEST(AddAndListNode)
	{
		ASSERT_TRUE(false);
	}

	UNIT_TEST(AddComponentsAndRemoveNode)
	{
		ASSERT_TRUE(false);
	}
}
